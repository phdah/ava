# Stable Release Guidance

Release guidance is transition-local and begins only after the stable `1.0.0` root release.

`1.0.0` has no source release and therefore no upgrade guidance. Each later stable release may add guidance only for its own immediately previous stable release edge, beginning with `1.0.0 -> 1.0.1`.
