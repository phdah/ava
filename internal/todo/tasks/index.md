# Internal roadmap tasks

This directory contains every native Backlog.md task in the Ava internal roadmap, regardless of lifecycle state.

Use Backlog.md as the task index. Query `To Do` tasks and dependencies through the CLI to select executable work. `Done`, `Won't Fix`, and `Parked` remain in this directory as normal task states and are not moved to a separate completed directory.
