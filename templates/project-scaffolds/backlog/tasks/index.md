# Project tasks

This directory contains the project's native Backlog.md task records.

Task lifecycle is represented by each task's `status`. Keep `To Do`, `In Progress`, `Done`, and `Won't Fix` tasks together here. Use the Backlog.md CLI or browser to query and manage task state.
