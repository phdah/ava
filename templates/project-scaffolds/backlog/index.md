# Project task board

This project uses Backlog.md-compatible native Markdown for project-owned task tracking.

- [Tasks](tasks/) contains all task records, including `Done` and `Won't Fix` tasks.
- `../backlog.config.yml` defines the board configuration.

Use the Backlog.md CLI or browser to query lifecycle state instead of maintaining a separate hand-written queue.
