# Installer Engine Source Fragments
#
# The numbered Python files are concatenated bytewise in lexical order during release assembly.
# This index is intentionally also valid Python comments because the complete directory is compiled as the packaged installer engine.
